apiVersion: v1
kind: ServiceAccount
metadata:
  name: hermes-agent
  namespace: hermes-agent
# Hermes does not call the Kubernetes API. No Role/RoleBinding is granted
# and the token is not mounted into the pod (also disabled at the pod level
# in deployment.yaml as belt-and-suspenders).
automountServiceAccountToken: false
